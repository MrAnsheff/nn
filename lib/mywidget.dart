import 'package:flutter/material.dart';

class MyWidget extends StatelessWidget{
  build(context){
    return new Container(
      padding: new EdgeInsets.all(32.0),
      child: new Text('My new widget'),
    );
  }
}